# Epistemic Monitor — environment variables
# Copy this file to .env.local and fill in the values.

# Base URL of the probabilistic-ontology-engine backend.
# Set this to your Railway deployment URL in production.
# If not set, defaults to http://localhost:8000 in development.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
